{
  "name": "nyc-parks-fields",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.12",
    "vite-plugin-singlefile": "^2.0.1",
    "vitest": "^1.6.0"
  }
}
